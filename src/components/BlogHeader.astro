---
// Sticky header for the blog (UZ / RU / EN). Self-contained so it works on the
// English pages too, which have no full marketing site behind them.
import { BLOG_UI, BLOG_LANGS, blogIndexPath, postPath, homePath, type BlogLang } from "../i18n/blog";
import { APP_URL } from "../i18n/copy";
import Logo from "./Logo.astro";
import Icon from "./Icon.astro";

interface Props { lang: BlogLang; slug?: string }
const { lang, slug } = Astro.props;
const ui = BLOG_UI[lang];
// language switch keeps you on the same article when reading one
const langHref = (l: BlogLang) => (slug ? postPath(l, slug) : blogIndexPath(l));
---

<header class="bnav">
  <div class="bnav-inner">
    <a href={homePath(lang)} class="bbrand" aria-label="Gempay">
      <Logo size={30} />
    </a>

    <nav class="bnav-links">
      <a href={homePath(lang)}>{ui.nav.home}</a>
      <a href={blogIndexPath(lang)} aria-current={!slug ? "page" : undefined}>{ui.nav.blog}</a>
    </nav>

    <div class="bnav-actions">
      <div class="blang" role="group" aria-label="Language">
        {BLOG_LANGS.map((l) => (
          <a href={langHref(l)} class:list={["blang-opt", { active: l === lang }]}>{l.toUpperCase()}</a>
        ))}
      </div>
      <a href={APP_URL} class="btn-primary bnav-play" target="_blank" rel="noopener">
        <Icon name="bolt" size={15} /> <span>{ui.nav.play}</span>
      </a>
    </div>
  </div>
</header>

<style>
  .bnav {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(7, 10, 20, 0.85);
    backdrop-filter: blur(14px);
    border-bottom: 1px solid var(--border-light);
  }
  .bnav-inner {
    max-width: var(--maxw);
    margin: 0 auto;
    padding: 12px 24px;
    display: flex;
    align-items: center;
    gap: 20px;
  }
  .bbrand { display: flex; align-items: center; }

  .bnav-links { display: flex; gap: 26px; margin-left: auto; }
  .bnav-links a {
    color: var(--text-muted);
    font-weight: 500;
    font-size: 0.95rem;
    transition: color 0.2s;
  }
  .bnav-links a:hover, .bnav-links a[aria-current="page"] { color: var(--text); }

  .bnav-actions { display: flex; align-items: center; gap: 14px; }
  .blang { display: inline-flex; border: 1px solid var(--border); border-radius: 10px; overflow: hidden; }
  .blang-opt {
    padding: 7px 11px;
    font-size: 0.78rem; font-weight: 700;
    color: var(--text-dim);
    transition: color 0.2s, background 0.2s;
  }
  .blang-opt:hover { color: var(--text); }
  .blang-opt.active { color: var(--text); background: rgba(124, 92, 255, 0.18); }
  .bnav-play { padding: 10px 20px; font-size: 0.9rem; box-shadow: 0 6px 20px rgba(124, 92, 255, 0.3); }

  @media (max-width: 720px) {
    .bnav-links { display: none; }
    .bnav-actions { margin-left: auto; }
  }
  @media (max-width: 460px) {
    .bnav-inner { padding: 12px 16px; gap: 12px; }
    .bnav-play span { display: none; }
    .bnav-play { padding: 10px 14px; }
    .bbrand :global(.logo-text) { display: none; }
  }
</style>
